ADD_SUBDIRECTORY(dsl)

IF(CUJ_ENABLE_LLVM)
	ADD_SUBDIRECTORY(native_codegen)
ENDIF()

IF(CUJ_ENABLE_CUDA AND CUJ_ENABLE_LLVM)
	ADD_SUBDIRECTORY(vec_add)
ENDIF()

IF(CUJ_ENABLE_CUDA AND CUJ_ENABLE_LLVM)
	ADD_SUBDIRECTORY(test)
ENDIF()
